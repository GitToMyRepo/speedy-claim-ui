import Navigation from '../Navigation/Navigation';
import './Header.css'

const Header = () => {
    return (
        <div className="header">
            <h1>Speedy Claim</h1>
            <Navigation/>
        </div>
    );
}

export default Header;